{
  "name": "xmcp-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "tsx scripts/gen-tools.ts",
    "dev": "wrangler dev",
    "deploy": "npm run gen && wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.90.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.12.3",
    "zod": "^4.0.0"
  }
}
